import { createTheme } from "@mui/material/styles";

export default createTheme({
  palette: {
    primary: { main: "#0F3D2E" },
    secondary: { main: "#A3D9A5" },
    background: { default: "#f6f9fb", paper: "#ffffff" },
  },
  typography: { fontFamily: "Inter, Roboto, sans-serif" },
});
